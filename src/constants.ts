import path from 'node:path';

export const BASE_DIR = path.join(__dirname, '..');
export const BASE_DOWNLOADS_DIR = path.join(BASE_DIR, 'downloads');
export const BASE_STITCHED_DIR = path.join(BASE_DIR, 'stitched');
